include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    - prefer_const_constructors
    - prefer_final_locals
    - avoid_print
    - sort_pub_dependencies
    # Enable once every public member is documented — pub.dev awards points for
    # API docs, and this lint is how you find the gaps:
    # - public_member_api_docs
